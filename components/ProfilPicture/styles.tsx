import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        padding: 10
    },
    image : {
        height: 45,
        width: 45,
        borderRadius: 40
    }
})

export default styles;